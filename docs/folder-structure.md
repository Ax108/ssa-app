# Folder structure

Paths are relative to this repository root ([Ax108/ssa-app](https://github.com/Ax108/ssa-app)).

Product code lives under **`src/`** (not Expo Router’s `app/` directory). Native shells `android/` and `ios/` are generated by prebuild.

## Repository root

```
ssa-app/
├── README.md                 # Project hub
├── docs/                     # This documentation set
├── package.json              # Scripts, deps, engines
├── app.json                  # Expo app config (name, ids, plugins)
├── app.config.js             # Dynamic config — OTA URL + runtimeVersion on top of app.json
├── eas.json                  # EAS Build / Submit profiles (not EAS Update)
├── index.ts                  # Metro entry → registerRootComponent
├── babel.config.js
├── metro.config.js
├── tsconfig.json             # Path aliases (@modules, @store, …)
├── tsconfig.no-tests.json    # Strict app-only typecheck (tsc:app)
├── eslint.config.js
├── jest.config.js / jest.setup.js
├── scripts/
│   └── publish-ota.mjs       # bun run ota:export:* → stage ssa-static OTA
├── plugins/                  # Expo config plugins (native tweaks)
│   ├── withAndroidLinkingQueries.js
│   └── android/
│       └── withGradleJvmArgs.js
├── assets/                   # App icon, splash, adaptive icon, logos
├── src/                      # All application TypeScript / UI
├── android/                  # GENERATED — do not hand-edit for features
└── ios/                      # GENERATED — macOS / Xcode
```

### Root rules

| Area | Rule |
|------|------|
| Config at root | Keep thin: Expo, Metro, ESLint, Jest, entry `index.ts` |
| `eas.json` | Native cloud builds + store submit only; JS OTA stays on ssa-static |
| `plugins/` | Expo config plugins only (e.g. Android intent queries) |
| `assets/` | Store branding binaries referenced from `app.json` |
| `android/` / `ios/` | Treat as output of `bun run prebuild`; regenerate when native deps/plugins change |
| `docs/` | Markdown for humans; no runtime imports |

## `src/` layout

```
src/
├── App.tsx                   # Splash gate: fonts + content init + OTA sync + ~500ms
├── appStore/                 # Zustand store (global — not under modules/)
│   ├── appStore.ts
│   ├── navbarSlice.ts
│   ├── contentSlice.ts
│   ├── storeUpdateSlice.ts   # Store binary update snackbar session state
│   └── contentController.ts  # Boot: AsyncStorage → sync/CDN (awaited at splash)
├── constants/                # palette, theme, CDN URLs
├── assets/
│   └── json/                 # Seed config + en/bn/hi (offline fallback)
├── modules/                  # One folder per product area
│   ├── app/                  # Splash, fonts, AppRoot shell
│   ├── navigation/           # Stacks, tab bar, top navbar, navigationRef, helpers/
│   ├── home/                 # GurujiRow, HomeHero, HomeScreen (+ Donate section)
│   ├── ashram/
│   ├── satsang/
│   ├── gallery/
│   ├── contact/
│   └── donation/             # Stack-only Donation screen (not a bottom tab)
├── shared/                   # Cross-module UI, hooks, utils, types, APIs
│   ├── components/           # FollowUsSection, AppFooterStrip, StoreUpdateSnackbar, …
│   ├── hooks/                # useNavigateTab, useNavigateDonation, …
│   ├── ota/                  # syncOtaUpdate, storeVersion, storeUpdateController
│   ├── helpers/
│   ├── serviceCalls/         # oxyApi (native fetch)
│   ├── types/
│   └── utils/                # assetUrl, openUrl, logger, formatConfig
└── tests/                    # Essential Jest suite only
```

## Module conventions

Each module under `src/modules/<name>/` should stay self-contained:

```
modules/home/
├── HomeScreen.tsx            # Screen entry used by navigation
├── components/               # Home-only UI pieces
└── data/                     # Optional local meta (not CDN JSON)
```

| Put here | Do not put here |
|----------|-----------------|
| Screen UI for that domain | Global Zustand slices → use `appStore/` |
| Module-only components | Shared buttons/images → `shared/components/` |
| Module path aliases (`@home/*`) | Native Android/iOS edits |

Same pattern for `ashram`, `satsang`, `gallery`, `contact`, `donation`. Navigation chrome stays in `modules/navigation/`.

## `appStore/` (global state)

- **`navbarSlice`** — active route title for **bottom tab** highlight (top bar is brand-only).
- **`contentSlice`** — `loaded`, `version`, `locale`, `config`, `texts`.
- **`storeUpdateSlice`** — session visibility for the store-update snackbar.
- **`contentController`** — async boot (cache → sync/CDN); awaited from `App.tsx` splash together with OTA sync.

Do not relocate the store into `modules/`.

## `shared/` vs `modules/`

| Kind of code | Location |
|--------------|----------|
| Used by 2+ modules | `src/shared/…` |
| Single-module UI | `src/modules/<module>/…` |
| HTTP helpers for CDN | `src/shared/serviceCalls/` |
| Types for config/texts | `src/shared/types/` |
| Theme / CDN constants | `src/constants/` |

## Path aliases (`tsconfig.json`)

| Alias | Maps to |
|-------|---------|
| `@store/*` | `src/appStore/*` |
| `@modules/*` | `src/modules/*` |
| `@shared/*` | `src/shared/*` |
| `@constants` / `@constants/*` | `src/constants` |
| `@home/*`, `@ashram/*`, … | Matching module folders |
| `@navigation/*` | `src/modules/navigation/*` |
| `@src/*` | `src/*` |
| `@/*` | Repo root |

Prefer aliases over deep relative `../../../` imports.

## Tests

- Unit / screen tests live under **`src/tests/`** (current layout).
- Run via `bun test` / `bun verify`.
- Details: [testing.md](./testing.md).

## What not to add

- Expo Router `app/` routes — this project uses React Navigation.
- Feature logic inside `android/` or `ios/`.
- Parallel roots like `src/screens`, `src/components` at the top of `src/` — use `modules/` and `shared/`.
